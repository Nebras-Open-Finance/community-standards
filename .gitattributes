# Normalize all text files to LF in the repo and in the working tree.
# Prevents CRLF/LF warnings on Windows and keeps diffs consistent across platforms.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
